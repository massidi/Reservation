<form method="post" action="{{ path('app_categorie_chambre_delete', {'id': categorie_chambre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_chambre.id) }}">
    <button class="btn">Delete</button>
</form>
